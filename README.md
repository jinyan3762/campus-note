# 校园记 (CampusNote)

> 专为大学生设计的一站式本地效率工具

## 项目简介

"校园记"是一款面向大学生群体的本地化桌面应用，集成了校园生活中最常用的六大功能模块。它解决了以下核心痛点：

- **交流需求**：通过内置校园论坛，学生可在本地网络环境内自由发帖、评论，形成紧密的校园社区。
- **日程管理**：提供课表查询、记事本和待办事项三大工具，覆盖从课程安排到日常笔记的全方位时间管理。
- **决策困难**：创新的"转盘决策"功能，帮助用户从待办事项中随机抽取一项执行，告别选择困难症。
- **社交匹配**：基于兴趣标签的搭子匹配系统，帮助学生找到志同道合的伙伴一起完成任务。

本项目完全本地运行，数据存储于 SQLite，无需联网，无需安装额外数据库或服务。

## 功能说明

### 模块一：用户系统
- 本地注册与登录，密码采用 SHA-256 加密存储
- 登录后进入主控面板，展示各功能模块入口和统计数据
- 支持编辑个人资料（昵称、兴趣标签）

### 模块二：校园论坛 (BBS)
- 浏览所有帖子列表（标题、作者、发布时间）
- 发布新帖子（标题 + 正文内容）
- 查看帖子详情，支持评论互动
- 作者可删除自己发布的帖子

### 模块三：校园记事本
- 便利贴风格的笔记卡片（多彩马卡龙色系背景）
- 支持增加、编辑、删除笔记
- 笔记按修改时间倒序排列
- 支持关键词搜索功能

### 模块四：课表查询
- 5×7 可视化课表网格（周一至周日 × 第1-5节课）
- 点击空格添加课程（课程名 + 教室）
- 支持编辑和删除已添加的课程
- 数据持久化存储，每人独立课表

### 模块五：待办事项 + 转盘决策（核心亮点）
- **普通模式**：添加/删除待办事项，标记完成/未完成
- **转盘模式**：
  - Canvas 绘制的彩色马卡龙转盘，每个扇区对应一个待办事项
  - 点击"随机抽取"，转盘带动画旋转并随机停止
  - 旋转结束高亮显示选中的待办事项，并滚动定位
  - **搭子联动**：每个待办事项旁有"找搭子做"按钮，点击自动跳转匹配模块

### 模块六：搭子匹配
- 个人资料卡（可编辑昵称、兴趣标签如"学习""运动""游戏""吃饭"等）
- **匹配算法**：综合兴趣标签重合度（Jaccard 相似度，权重 70%）+ 最近活跃度（权重 30%），按匹配度降序展示前 3 名用户
- 支持向匹配到的用户发送"组队邀请"，可接受/拒绝收到的邀请
- 从待办事项跳转时自动触发匹配

## 运行方法

### 环境要求
- Python 3.9+

### 步骤

```bash
# 步骤 1：安装依赖（在PowerShell窗口执行）
pip install -r requirements.txt

# 步骤 2：启动应用（在PowerShell窗口执行）
python app.py

# 步骤 3：打开浏览器访问
# http://127.0.0.1:5000
```

首次运行时，数据库会自动初始化并创建默认管理员账号：
- 用户名：`admin`
- 密码：`123456`

### 依赖项
| 包 | 版本 | 用途 |
|---|---|---|
| Flask | 3.0.0 | Web 框架 |
| Werkzeug | 3.0.1 | WSGI 工具库 |

## AI 使用说明

本项目由 **Claude Code**（Anthropic 的 AI 编程助手）辅助生成。

### 提示词设计思路

项目构建指令（`校园记.md`）遵循以下设计原则：

1. **结构化需求描述**：将项目需求分为技术栈、功能模块、Git 操作和文档四大板块，确保 AI 能准确理解全貌。
2. **模块化拆分**：六大功能模块各自独立描述，附带具体的交互细节和存储要求，便于 AI 分步实现。
3. **明确的约束条件**：指定 UI 风格（马卡龙色系）、技术栈（Flask + 原生前端）和运行方式，减少歧义。
4. **分步提交策略**：要求 5 次有意义的 Git Commit，使开发过程可追溯。

### Claude Code 在关键功能中的作用

1. **Canvas 转盘绘制与动画**：
   - 使用 HTML5 Canvas API 绘制环形转盘，每个扇区着色为不同的马卡龙色系
   - 通过 `requestAnimationFrame` 实现平滑的缓出（ease-out）旋转动画
   - 随机目标角度计算确保结果不可预测
   - 旋转结束后高亮结果并自动滚动定位

2. **搭子匹配算法**：
   - 基于 Jaccard 相似度计算兴趣标签重合度
   - 综合活跃度因子（在线状态 + 最近活跃时间）进行加权排序
   - 返回匹配度最高的前 3 名用户

3. **SPA 前端路由**：
   - 基于 URL Hash 的单页应用路由，无需刷新页面即可切换模块
   - 未登录拦截重定向

### 如何进行二次开发

你可以继续使用 Claude Code 对本项目进行功能扩展。在项目根目录下，使用以下方式：

```bash
# 启动 Claude Code
claude

# 在对话中描述你想要的修改，例如：
# "帮我在论坛模块添加点赞功能"
# "将记事本改为 Markdown 编辑器"
# "添加暗黑模式切换"
```

Claude Code 会读取项目代码，理解现有架构，并在保持代码风格一致的前提下实现新功能。

---

## 项目结构

```
校园记软件/
├── app.py                  # Flask 主入口（路由 + 数据库初始化）
├── requirements.txt        # Python 依赖
├── README.md               # 项目文档
├── .gitignore
├── database/               # SQLite 数据库目录（自动生成）
├── static/
│   ├── css/
│   │   └── style.css       # 全局样式（马卡龙色系）
│   └── js/
│       ├── api.js           # 通用工具 + 主控面板
│       ├── auth.js          # 用户认证与资料编辑
│       ├── forum.js         # 论坛 BBS
│       ├── notepad.js       # 记事本
│       ├── schedule.js      # 课表查询
│       ├── todo.js          # 待办事项 + Canvas 转盘
│       └── partner.js       # 搭子匹配
└── templates/
    ├── base.html            # 基础布局（导航栏 + 路由系统）
    ├── index.html           # 登录/注册页
    ├── dashboard.html       # 主控面板
    ├── forum.html           # 论坛
    ├── notepad.html         # 记事本
    ├── schedule.html        # 课表
    ├── todo.html            # 待办 + 转盘
    └── partner.html         # 搭子匹配
```

## 更新日志

### 2026-06-23 — v1.0.1
- **修复**：转盘旋转后指针指向的扇区与底部高亮显示的事项不一致的问题。原因是目标角度计算将选中扇区对准了 3 点钟方向（0 弧度），而 CSS 指针位于转盘顶部（-π/2 弧度），现已修正角度偏移计算。

### v1.0.0
- 初始版本，包含六大功能模块：用户系统、校园论坛、记事本、课表查询、待办+转盘决策、搭子匹配。

---

## License

MIT License
